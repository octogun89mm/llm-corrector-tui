[project]
name = "llm-corrector-tui"
version = "0.1.0"
description = "Small terminal text corrector for llama.cpp on Hyprland"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["src"]
